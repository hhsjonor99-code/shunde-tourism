# CLAUDE.md

## 项目定位

**《一卷顺德》** —— 一个水墨风顺德城市旅游网站。
副标题：水乡入画，寻味成诗。

## 工作目录

```
E:\Coding\claud code\shundeTourism
```

## 文档结构

| 文档 | 内容 |
|------|------|
| `docs/product-design.md` | 产品定位、5+2 路线总表、推荐引擎规则 |
| `docs/mcp-verification.md` | 高德 MCP 核验数据（景点、路段、出发地点） |
| `docs/ui-style-guide.md` | 视觉、字体、动效、响应式、无障碍 |
| `docs/development-record.md` | 技术初始化、数据结构、推荐规则 |

## 第一阶段边界（严格遵守）

### ✅ 必做（已完成于阶段 1）
- 5 条预制主题路线 R1-R5 + 2 条半天变体 H1/H2
- "为我画一卷行程"本地关键词匹配推荐
- 首屏水墨长卷
- 顶部导航 + 页脚
- 初识顺德·四主题、顺德印象·四组、关于项目
- 响应式（桌面 + 平板 + 手机）
- 6 种字体 + 留白 + 朱红印章 + 笔触装饰
- 动效（克制版）

### ❌ 第一版不实现
- 大模型调用 / 后端 / 高德 Web Key / JS API 互动地图
- 实时天气 / 实时公交 / 一键高德导航
- 用户账号 / 收藏 / 分享 / 中英双语
- 复杂视差 / 粒子 / PWA

## 关键规则（重要）

### 推荐引擎候选池
**仅允许在以下 7 条预定义路线之内评分匹配**：
- R1 初见顺德·大良经典一日游
- R2 水乡慢游·逢简寻味一日
- R3 园林烟火·顺德文化一日游
- R4 艺术与夜色·顺德城市一日游
- R5 两日寻味顺德
- H1 大良老城·寻味半日
- H2 山水夜色·休闲半日

不得产生未经 MCP 核验的新路线顺序。

### 总里程范围
`totalDistance` / `totalTransportTime` 只统计相邻景点之间的已核验路段，**不包括**出发地接驳、住宿、候车、停车和景区内部游览时间。

### `verificationStatus` 三值
- `verified` —— MCP 已核验
- `unverified` —— 未核验，包括用户自定义起点产生的接驳路段
- `not-applicable` —— 无需路线计算（同一景点内部活动或景区内自由参观）

**用户自定义接驳路段必须标 `unverified`，不得标 `not-applicable`。**
**不允许使用 `estimated`。**

### 公共交通边界
- 不展示精确公交时间；
- 不调用 `maps_direction_transit_integrated`；
- 自驾显示驾车时间；打的显示"驾车路线参考时间，不含候车"；公共交通显示"暂无精确公交耗时"；
- 严禁把驾车时间当成公交时间。

### 自由文本
- 第一版是关键词匹配，非大模型自然语言理解；
- 未识别文本原样回显 + 提示文字。

## 安全红线

- 🚫 不出现任何 API Key / Token；
- 🚫 不写高德 Web 服务 Key 到前端代码；
- 🚫 不在浏览器端调用大模型；
- 🚫 不执行 `git init` / `git commit`（除非用户明确要求）；
- 🚫 不删除项目外任何文件。

## 数据时效

所有地点、营业时间和路线数据于 **2026-07-18** 通过高德地图 MCP 查询。
实际开放时间、天气和交通情况请在出发前再次核验。

## 当前阶段

**阶段 1（项目初始化）已完成**：Vite + React + TypeScript 初始化、`npm install`、4 篇 docs 文档、基础目录结构、全局 CSS 变量、宣纸白背景。
**阶段 2-8 暂未开始**，等待用户批准实施。